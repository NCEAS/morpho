keywords page needs multi-column list.  
 - Then need keyword dialog
 
3 party pages need adding
 - then need party dialog
 
 file location page:
 - inline (import) radiobutton need implementing (text import wiz)
 - online/offline - generate info rb needs implementing (text import wiz)
 
entities & attributes pages & dialogs to add
- table name becomes entityName at beginning of datatable tag