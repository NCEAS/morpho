<?xml version="1.0"?>
<!-- 
  * build.xml
  *
  *      Authors: Matt Jones
  *    Copyright: 2000 Regents of the University of California and the
  *               National Center for Ecological Analysis and Synthesis
  *  For Details: http://www.nceas.ucsb.edu/
  *    File Info: '$Id: build.xml,v 1.9 2000-09-26 15:05:36 higgins Exp $'
  *
  *  Build file for the Ant cross-platform build system for metacat
  *  See http://jakarta.apache.org for details on Ant
  *
  * usage: ant [compile|jar|install|jdoc]
--> 
<project name="metacat" default="jar" basedir=".">
   <target name="init">
      <property name="installdir" value="/opt/tomcat/webapps/metadata" />
      <property name="name" value="dmanclient"/>
      <property name="Name" value="Data Management Client"/>
      <property name="release" value="0.00"/>

      <filter token="release" value="${release}"/>

      <property name="srcdir" value="./src" />
      <property name="libdir" value="./lib" />

      <property name="build.dir" value="./build"/>
      <property name="build.src" value="${build.dir}/src"/>
      <property name="build.dest" value="${build.dir}/classes"/>
      <property name="build.docs" value="${build.dir}/docs/html"/>
      <property name="build.samples" value="${build.dir}/samples"/>
      <property name="build.tests" value="${build.dir}/tests"/>
      <property name="build.data" value="${build.dir}/data"/>
      <property name="build.javadocs" value="${build.dir}/docs/apiDocs"/>

      <property name="xml4j" 
                value="${libdir}/xml4j.jar" />
      <property name="xerces" 
                value="${libdir}/xerces.jar" />
      <property name="xalan" 
                value="${libdir}/xalan.jar" />
      <property name="sym" 
                value="${libdir}/sym.jar" />
      <property name="cpath" 
                value="${xml4j}:${xerces}:${xalan}:${build.dest}:${sym}"/>
      <property name="package.home" value="edu/ucsb/nceas" />

   </target>
   
   <target name="prepare" depends="init">
      <mkdir dir="${build.dir}"/>
      <mkdir dir="${build.src}"/>
      <mkdir dir="${build.dest}"/>

      <copydir src="${srcdir}" 
               dest="${build.src}" 
               excludes="**/CVS* **/.#* "/>
   </target>

   <target name="compile" depends="prepare">

      <javac srcdir="${build.src}" 
             destdir="${build.dest}" 
             classpath="${cpath}" /> 
   </target> 

   <target name="jar" depends="compile">
       <delete file="${build.dir}/${name}.jar" />
       <copydir src="${srcdir}/${package.home}/metaedit/images" 
                dest="${build.dest}/${package.home}/metaedit" />
       <copydir src="${srcdir}/${package.home}/dtclient/images" 
                dest="${build.dest}/${package.home}/dtclient" />
       <copydir src="${srcdir}/${package.home}/querybean/images" 
                dest="${build.dest}/${package.home}/querybean" />
       <jar jarfile="${build.dir}/${name}.jar" basedir="${build.dest}" />
   </target> 

   <target name="install" depends="jar">
       <copyfile src="${build.dir}/${name}.jar" 
                 dest="${libdir}/${name}.jar" />
       <echo message="Install complete. Use the dmanclient script to run it." />
   </target> 

   <target name="clean" depends="init">
       <deltree dir="${build.dir}" />
   </target>

   <target name="jdoc" depends="init">
       <mkdir dir="${build.javadocs}" />
       <javadoc packagenames="edu.ucsb.nceas"
           sourcepath="src"
           destdir="${build.javadocs}"
           author="true"
           version="true"
           use="true"
           windowtitle="${Name} API"
           doctitle="&lt;h1&gt;${Name}&lt;/h1&gt;"
           bottom="&lt;i&gt;Copyright &#169; 2000 National Center for Ecological Analysis and Synthesis. All Rights Reserved.&lt;/i&gt;"
       />
   </target>

</project>
